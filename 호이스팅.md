# 호이스팅 (Hoisting)

<br />
<br />

```
호이스팅은 변수나 함수의 선언을
스코프 최상단으로 끌어올린 것처럼 동작하게 하는 자바스크립트의 고유한 특징이다.
```

<br /><br /><br />

1. 변수, 함수 선언

```
변수나 함수 선언이 실행되기 전, 해당 스코프의 맨 위로 끌어올려진 것처럼 작동하는 현상을 의미한다.

var로 선언한 변수는 undefined로 초기화되고,
함수 선언은 함수 전체가 끌어올려져 선언보다 아래에서 호출해도 오류가 발생하지 않는다.

이러한 혼란을 피하기 위해 많은 개발자들이 변수는 항상 스코프 최상단에 선언하고,
var보다는 실수를 방지해주는 let과 const를 사용하고 있다.
```

<br />

`var 변수 사용 예시`

```js
console.log(myVar); // undefined

var myVar = 5;

console.log(myVar); // 5
```

<br />

`함수 선언 예시`

```js
console.log(myFunc()); // Hello, World!

function myFunc() {
  return "Hello, World!";
}
```

<br />

`함수 표현식`

```js
// 1) var 선언부가 최상단으로 호이스팅되고 undefined로 초기화됨
var sayHello;

// 2) 변수의 현재 값 확인
console.log(sayHello); // undefined 출력

// 3) 함수 호출 시도 -> undefined()를 실행하려는 것과 같음
sayHello(); // TypeError 발생

// 4) 원래 위치에서 함수가 변수에 할당됨
sayHello = function() {
  console.log("안녕하세요!");
};

// 5) 정상 호출
sayHello();
```

<br />
<br />
<br />

2. let, const, function expression

```
ES6부터 도입된 let과 const는 블록 스코프를 가지며,
호이스팅되지만 초기화되지 않은 상태로 접근할 경우 ReferenceError가 발생한다.

함수 표현식의 경우 변수에 대한 호이스팅은 있지만,
초기화는 이루어지지 않아 undefined 상태로 남아 있기 때문에 에러가 발생한다.
```
